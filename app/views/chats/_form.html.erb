<%= form_with(model: chat, local: true) do |form| %>
  <% if chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat.errors.count, "error") %> prohibited this chat from being saved:</h2>

      <ul>
        <% chat.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :history %>
    <%= form.text_field :history %>
  </div>

  <div class="field">
    <%= form.label :q_and_a %>
    <%= form.text_field :q_and_a %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
